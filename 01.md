# CORD-00: Private Stuff with No Servers

`draft` `optional`

A primitive for sending end-to-end encrypted, self-addressed events over Nostr among a
set of parties who share a symmetric key.

:exploding_head:

TOTEMS.

CHANNELS BY SHARED KEYS

EVERYONE HAS THE SECRET

NOW IT'S PRIVATE GROUPS WITH NO SERVERS

```json
{
  "kind": 1059,
  "id": "<id>",
  "pubkey": "<channel pubkey>",
  "tags": [["p", "<self ephemeral key>"]],
  "content": "nip44_encrypt({
    "id": "<id>",
    "pubkey": "<real author's pubkey>",
    "content": "nip44_encrypt({
      "id": "<id>",
      "kind": 9,
      "pubkey": "<real author's pubkey>",
      "content": "Hey chat!",
      "created_at": 1686840217,
    })",
    "kind": 13,
    "created_at": 1686840217,
    "tags": [],
    "sig": "<real author's pubkey signature>"
  })",
  "created_at": 1686840217,
  "sig": "<random, one-time-use pubkey signature>"
}
```

- The created_at isn't random
- self ephemeral key can be used to delete your own giftwraps
- The channel key is what matters.

You query by:
Build applications around invite links. The invite link either contains the channel secret, or a way to otherwise get or derive it.

```json
{"authors": ["<channelPubkeys>"]}
```

Build applications around sending people invite links. The invite link either contains the channel secret, or a way to otherwise get or derive it.